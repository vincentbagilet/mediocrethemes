---
title: "R Notebook"
output: html_notebook
---

```{r}
library(tidyverse)
library(mediocrethemes)

set_mediocre_all()
```


This is an [R Markdown](http://rmarkdown.rstudio.com) Notebook. When you execute code within the notebook, the results appear beneath the code. 

Try executing this chunk by clicking the *Run* button within the chunk or by placing your cursor inside it and pressing *Cmd+Shift+Enter*. 

```{r}
  ggplot(data = ggplot2::mpg, aes(x = cty, y = displ)) +
    geom_col() +
    # theme_mediocre() +
    # scale_mediocre_d() +
    labs(title = "A very nice title")#, subtitle = "A disapointing subtitle")
  
```

```{r}
cars %>% 
  ggplot(aes(speed, dist)) +
  geom_point() +
  # theme_mediocre() +
  # scale_mediocre_d() +
  labs(title = "A very nice title", subtitle = "A disapointing subtitle", x = "This is a quite long name for an x axis", y = "This is a quite long name for a y axis")
```


